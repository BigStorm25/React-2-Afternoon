import React, { Component } from 'react';
import data from './data';

// const counter = ({props}) => {
//     return (
//     <h1>{data.id}/{data.id.length}</h1>
//     )
// }
const counterChange = (e) {
    this.setState({id: counter})
}


export default counter