import React, { Component } from 'react';
import counter from './counter';
import data from './data';

class faveMovies extends Component() {
    constructor() {
        super()
        this.state = {
            id: {counter}
        }
    }
    // counterChange(e) {
    //     this.setState({id: counter})
    // }
    render(data) {
        return <ol>{}</ol>
    }
}

export default faveMovies